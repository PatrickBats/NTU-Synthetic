# Complete requirements for NTU-Synthetic project - Conda compatible
# Removes version conflicts

# Core ML/DL frameworks (already installed via conda)
# torch==2.0.1
# torchvision==0.15.2
pytorch_lightning==2.3.3

# Transformers and Diffusion models
transformers==4.51.3
diffusers
accelerate
timm

# Computer Vision (some already installed)
# opencv-python==4.10.0.84
opencv-python-headless
# Pillow==10.4.0
# imageio==2.35.0
scikit-image

# Scientific Computing (already installed)
# numpy==1.24.4
# scipy==1.10.1
# pandas==2.0.3
# matplotlib==3.7.5
seaborn

# NLP
spacy==3.7.5
# openai-clip==1.0.1

# Tensor Operations
einops

# Google APIs
google_api_python_client==2.141.0
google_auth_oauthlib==1.2.1
gsheets==0.6.1

# Utilities
# tqdm==4.66.5
openpyxl
# protobuf==5.27.3
# huggingface_hub - let pip resolve the version
huggingface-hub>=0.30.0
omegaconf
python-dotenv
ninja
# wheel

# Evaluation
# pycocoevalcap==1.2

# Monitoring (optional)
wandb

# Jupyter support
ipykernel
jupyter
notebook
ipywidgets

# Platform specific
triton-windows

# Optional performance improvements (may require manual installation)
# flash-attn  # Linux only, requires CUDA toolkit